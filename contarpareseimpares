# Contar cuántos números pares e impares hay del 1 al 10

pares = 0
impares = 0
for num in range(1, 11):
    if num % 2 == 0:
        pares += 1
    else:
        impares += 1

print(f"Cantidad de pares: {pares}")
print(f"Cantidad de impares: {impares}")
